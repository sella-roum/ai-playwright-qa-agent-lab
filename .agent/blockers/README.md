# Agent Blockers

This directory stores safe human-review blocker records.

When the full quality gate cannot be repaired inside a workflow run, the agent reverts broken working-tree changes and commits only the blocked state plus a blocker summary here.
