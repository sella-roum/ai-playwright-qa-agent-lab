# Flaky Patterns

## 既知の原因候補

- IndexedDB初期化を待たずに一覧を検証する
- 保存後のUI更新を固定waitで待つ
- 前テストのデータが残っている
- 疑似ロールやエラー注入の設定が残っている

## 対策

- `?reset=1` を使って初期状態を作る
- Web First Assertionで状態変化を待つ
- テストごとに明示的なデータセットを指定する
